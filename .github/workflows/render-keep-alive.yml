name: Render Keepalive

on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes
  workflow_dispatch:

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl --fail --silent --show-error https://pharmacy-pos-6hw9.onrender.com/admin
